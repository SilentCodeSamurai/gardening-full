/**
 * Form default for “no selection yet”. Keep real options only in `values`;
 * `field.Select` maps this to an unset Radix value so `SelectValue` shows `placeholder`.
 */
export const SELECT_NONE = "__none__" as const;
