export const DEFAULT_LOCATION_RECT = {
	x: 0,
	y: 0,
	width: 600,
	height: 400,
} as const;

export const DEFAULT_PLANT_RECT = {
	x: 0,
	y: 0,
	width: 80,
	height: 80,
} as const;

export const DEFAULT_LAYOUT_SNAP = {
	x: 20,
	y: 20,
} as const;
